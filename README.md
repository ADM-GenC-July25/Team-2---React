# Contribution

Clone the repository.
git clone {url}.
git pull.

Install the packages.
npm install.

Run the project.
npm run dev.

Always pull.
Everyone in a separate branch.
Merge when feature is done.
Merge to Main.
